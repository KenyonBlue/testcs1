import { AlertButtonComponent } from './alert-button.component';

describe('AlertButtonComponent', () => {

it('should create the component', () => {
  expect(AlertButtonComponent).toBeTruthy();
});


});